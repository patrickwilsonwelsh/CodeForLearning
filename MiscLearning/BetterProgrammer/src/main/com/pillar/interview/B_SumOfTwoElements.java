package com.pillar.interview;


public class B_SumOfTwoElements {

    /**
     * Please implement this method to
     * return the sum of the two elements closest to zero.
     * If there are two elements equally close to zero like -2 and 2,
     * consider the positive element to be "closer" to zero than the negative one.
     * If the the input is null, or contains fewer than two elements, throw an
     * IllegalArgumentException.
   */
	public int getSumOfTwoClosestToZeroElements (int[] a) {
		// TODO Auto-generated method stub
		return 0;
	}
}
